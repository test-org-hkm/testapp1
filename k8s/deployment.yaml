apiVersion: apps/v1
kind: Deployment
metadata:
  name: himanshu-ka-domain
  labels:
    app: himanshu-ka-domain
  namespace: utils
spec:
  replicas:1
  selector:
    matchLabels:
      app: himanshu-ka-domain
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: himanshu-ka-domain
    spec:
      containers:
      - name: himanshu-ka-domain
        image: 
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8000
        resources:
          limits:
            cpu: 200m
            memory: 512Mi
          requests:
            cpu: 200m
            memory: 512Mi
        envFrom:
        - secretRef:
            name: himanshu-ka-domain-secrets
        env:
        - name: REDIS_HOST
          value: "redis-service"
        - name: REDIS_PORT
          value: "6379"
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 5 